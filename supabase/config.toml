# Supabase project configuration.
# Link a remote project with:  supabase link --project-ref <ref>
# Apply migrations locally:    supabase db reset   (or `supabase migration up`)
# Apply migrations remotely:   supabase db push
project_id = "fefo-allocator"

[db]
major_version = 15

[studio]
enabled = false
